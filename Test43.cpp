#include <iostream>
using namespace std;

int main(){
	
	int a, b, r, s;
	cin>>a>>b>>r>>s;
	
	if(a >= 0 && b > 0);
	
	if((a % b == r) || (a % b == s)) cout<<"YES";
	else cout<<"NO";	
	
	
	
	return 0;
}